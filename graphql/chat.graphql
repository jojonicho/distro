subscription Chat {
  newMessage {
    id
    user {
      username
      image
    }
    date
    content
  }
}

mutation SendMessage($content: String!) {
  sendMessage(input: { content: $content })
}

mutation DeleteMessage($id: Int!) {
  deleteMessage(id: $id)
}
